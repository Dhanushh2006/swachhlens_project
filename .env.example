# Leave unset to use the reliable IndexedDB-backed prototype mode.
# Configure both values to connect the application to your Supabase project.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional label shown in the UI (never put service-role keys in Vite env variables).
VITE_APP_ENV=prototype
