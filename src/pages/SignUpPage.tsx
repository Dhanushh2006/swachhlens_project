import { useState, type FormEvent } from 'react'
import { ArrowLeft, UserPlus } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { Logo } from '../components/brand/Logo'
import { Button } from '../components/ui/Button'
import { useAuth } from '../context/AuthContext'

export function SignUpPage(){const {signUp}=useAuth();const navigate=useNavigate();const [form,setForm]=useState({name:'',email:'',password:''});const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await signUp(form.name,form.email,form.password);navigate('/app')}catch(err){setError(err instanceof Error?err.message:'Account could not be created.')}finally{setBusy(false)}};return <main className="grid min-h-screen place-items-center bg-canvas px-5 py-10"><div className="w-full max-w-md"><Link to="/login" className="flex items-center gap-2 text-xs font-bold text-forest-700"><ArrowLeft size={14}/>Back to login</Link><div className="card mt-5 rounded-3xl p-6 sm:p-8"><Logo/><p className="eyebrow mt-8">Citizen registration</p><h1 className="mt-2 text-2xl font-black">Create your account</h1><p className="mt-2 text-sm leading-6 text-slate-500">Citizen accounts can submit reports and see only their own protected incident details.</p><form onSubmit={submit} className="mt-6 space-y-4"><div><label className="label" htmlFor="name">Full name</label><input id="name" className="input" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div><label className="label" htmlFor="signup-email">Email</label><input id="signup-email" type="email" className="input" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div><label className="label" htmlFor="signup-password">Password</label><input id="signup-password" type="password" className="input" minLength={8} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><p className="mt-1.5 text-[10px] text-slate-500">Minimum 8 characters. Never use a demo password in production.</p></div>{error&&<p role="alert" className="rounded-lg bg-red-50 p-3 text-xs font-semibold text-red-700">{error}</p>}<Button className="w-full" size="lg" loading={busy} icon={<UserPlus size={17}/>}>Create citizen account</Button></form></div></div></main>}
