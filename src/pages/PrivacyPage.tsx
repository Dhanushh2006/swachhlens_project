import { ArrowLeft, Camera, Database, EyeOff, MapPin, ShieldCheck, UserRound } from 'lucide-react'
import { Link } from 'react-router-dom'
import { Logo } from '../components/brand/Logo'

export function PrivacyPage(){return <main className="min-h-screen bg-canvas"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex h-20 max-w-5xl items-center justify-between px-5"><Logo/><Link to="/" className="flex items-center gap-2 text-xs font-bold text-forest-700"><ArrowLeft size={15}/>Home</Link></div></header><article className="mx-auto max-w-3xl px-5 py-12"><p className="eyebrow">Privacy by design</p><h1 className="mt-3 text-4xl font-black tracking-tight">Context for response.<br/>Not citizen surveillance.</h1><p className="mt-5 text-base leading-7 text-slate-600">SWACHHLENS is designed to collect the minimum information needed to understand and resolve a waste incident.</p><div className="mt-10 grid gap-4 sm:grid-cols-2">{[[Camera,'Media','Used to classify visible waste and verify cleanup. Users are asked to avoid identifiable faces.'],[MapPin,'Location','Used only for incident context, duplicate detection, routing and sensitivity signals.'],[UserRound,'Identity','Citizen identity is not shown on public incident views or shared with recycling partners.'],[Database,'Retention','Production deployments should define municipal retention windows and protected Storage policies.']].map(([Icon,title,copy])=><section className="card rounded-2xl p-5" key={String(title)}><Icon className="text-forest-700"/><h2 className="mt-4 font-black">{title as string}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{copy as string}</p></section>)}</div><section className="mt-8 rounded-2xl bg-forest-800 p-6 text-white"><div className="flex items-center gap-3"><EyeOff/><h2 className="text-xl font-black">What we do not build</h2></div><ul className="mt-5 grid gap-3 text-sm text-forest-100 sm:grid-cols-2"><li>• No facial recognition</li><li>• No citizen risk profiling</li><li>• No public citizen identity</li><li>• No sale of report data</li></ul></section><section className="mt-8 card rounded-2xl p-6"><ShieldCheck className="text-forest-700"/><h2 className="mt-3 text-xl font-black">Access control and accountability</h2><p className="mt-3 text-sm leading-6 text-slate-600">Supabase Row Level Security policies enforce role-scoped access. Protected media should be served through short-lived signed URLs. Assignment, status and resolution changes create audit log records.</p></section><p className="mt-8 text-xs leading-5 text-slate-500">Prototype note: this competition build uses fictional/demo incident records and generated demonstration images. It does not claim official government integration or production deployment approval.</p></article></main>}
