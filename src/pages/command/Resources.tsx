import { CheckCircle2, MapPin, Truck, UsersRound, Wrench } from 'lucide-react'
import { Badge } from '../../components/ui/Badge'
import { useData } from '../../context/DataContext'

export function Resources({type}:{type:'teams'|'vehicles'}){const {teams,vehicles}=useData();const items=type==='teams'?teams:vehicles;return <div className="p-4 sm:p-6 lg:p-8"><p className="eyebrow">Response capacity</p><h1 className="mt-2 text-2xl font-black sm:text-3xl">{type==='teams'?'Teams':'Vehicles'}</h1><p className="mt-2 text-sm text-slate-500">Live assignment availability for operational matching.</p><div className="mt-7 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map((raw)=>{const isTeam='teamType'in raw;return <article key={raw.id} className="card rounded-2xl p-5"><div className="flex items-start justify-between"><span className="grid h-11 w-11 place-items-center rounded-xl bg-forest-50 text-forest-700">{isTeam?<UsersRound size={21}/>:<Truck size={21}/>}</span><Badge tone={raw.availability==='AVAILABLE'?'success':raw.availability==='ASSIGNED'?'high':'neutral'}>{raw.availability}</Badge></div><h2 className="mt-4 font-black">{raw.name}</h2><p className="mt-1 text-xs font-semibold text-slate-500">{isTeam?raw.teamType:raw.vehicleType}</p><div className="mt-5 space-y-3 border-t border-slate-100 pt-4 text-xs">{isTeam?<p className="flex items-center gap-2"><CheckCircle2 size={14} className="text-forest-600"/><b>{raw.workerCount}</b> trained workers</p>:<p className="flex items-center gap-2"><Wrench size={14} className="text-forest-600"/>Capacity: <b>{raw.capacity}</b></p>}<p className="flex items-center gap-2 text-slate-500"><MapPin size={14}/>Live location available to dispatcher</p></div></article>})}</div></div>}
