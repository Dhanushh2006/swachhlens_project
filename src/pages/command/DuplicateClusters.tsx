import { ArrowRight, Check, Images, MapPin, Network } from 'lucide-react'
import { Link } from 'react-router-dom'
import { Badge } from '../../components/ui/Badge'
import { IncidentMedia } from '../../components/ui/IncidentMedia'
import { useData } from '../../context/DataContext'
import { pct } from '../../lib/utils'

export function DuplicateClusters(){const {clusters,incidents}=useData();return <div className="p-4 sm:p-6 lg:p-8"><div><p className="eyebrow">Duplicate intelligence</p><h1 className="mt-2 text-2xl font-black sm:text-3xl">One issue → one response</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">GPS proximity, time, waste category and a pluggable image-similarity adapter consolidate citizen signals into master incidents.</p></div><div className="mt-7 grid gap-5 xl:grid-cols-2">{clusters.map(cluster=>{const master=incidents.find(i=>i.id===cluster.masterIncidentId);if(!master)return null;return <article className="card overflow-hidden rounded-2xl" key={cluster.id}><div className="flex items-center justify-between border-b border-slate-100 bg-slate-50 px-5 py-3"><div className="flex items-center gap-2"><Network size={16} className="text-forest-700"/><b className="text-xs">{cluster.id}</b></div><Badge tone="info">{pct(cluster.similarityScore)} similarity</Badge></div><div className="grid sm:grid-cols-[160px_1fr]"><div className="relative"><IncidentMedia url={master.mediaUrl} type={master.mediaType} className="h-full min-h-44 w-full" alt="Master incident evidence"/><span className="absolute bottom-2 left-2 rounded bg-black/70 px-2 py-1 text-[9px] font-bold text-white">MASTER</span></div><div className="p-5"><div className="flex justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-widest text-slate-400">Master incident</p><h2 className="mt-1 text-lg font-black">{master.id}</h2></div><div className="text-right"><b className="text-3xl text-forest-700">{master.reportCount}</b><p className="text-[9px] font-bold uppercase text-slate-400">citizen reports</p></div></div><p className="mt-3 text-sm font-bold">{master.wasteCategory}</p><p className="mt-1 flex items-center gap-1 text-xs text-slate-500"><MapPin size={12}/>{master.address}</p><div className="mt-4 space-y-1.5">{cluster.detectionReason.map(reason=><p key={reason} className="flex items-center gap-2 text-[11px] text-slate-600"><Check size={12} className="text-forest-600"/>{reason}</p>)}</div><Link to={`/command/incidents/${master.id}`} className="mt-4 flex items-center gap-1 text-xs font-black text-forest-700">Open consolidated incident <ArrowRight size={13}/></Link></div></div><div className="flex items-center gap-2 border-t border-slate-100 px-5 py-3 text-[11px] font-semibold text-slate-500"><Images size={14}/>{cluster.memberIncidentIds.length} linked incident records · citizen identities protected</div></article>})}</div></div>}
