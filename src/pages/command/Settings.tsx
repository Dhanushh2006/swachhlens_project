import { BrainCircuit, Database, Gauge, LockKeyhole, SlidersHorizontal } from 'lucide-react'
import { ACTION_SCORE_CONFIG } from '../../config/decisionRules'
import { Badge } from '../../components/ui/Badge'

export function Settings(){return <div className="p-4 sm:p-6 lg:p-8"><p className="eyebrow">Transparent configuration</p><h1 className="mt-2 text-2xl font-black sm:text-3xl">Decision settings</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Prototype rule definitions are centralized and inspectable. Production changes should require audited administrative approval.</p><div className="mt-7 grid gap-6 lg:grid-cols-2"><section className="card rounded-2xl p-5"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-lg bg-forest-50 text-forest-700"><Gauge/></span><div><h2 className="font-black">ActionScore™ thresholds</h2><p className="text-xs text-slate-500">0–100 operational urgency</p></div></div><div className="mt-5 space-y-3">{Object.entries(ACTION_SCORE_CONFIG.thresholds).map(([key,value])=><div className="flex items-center justify-between rounded-lg bg-slate-50 p-3" key={key}><Badge tone={key==='critical'?'critical':key==='high'?'high':'medium'}>{key}</Badge><b className="text-sm">≥ {value}</b></div>)}</div></section><section className="card rounded-2xl p-5"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-lg bg-blue-50 text-blue-700"><SlidersHorizontal/></span><div><h2 className="font-black">Scoring factors</h2><p className="text-xs text-slate-500">Maximum contribution</p></div></div><div className="mt-5 space-y-3">{[['Waste volume',25],['Location sensitivity',25],['Citizen report frequency',20],['Complaint age',15],['Hazard / context',15]].map(([k,v])=><div key={String(k)}><div className="flex justify-between text-xs"><span>{k}</span><b>{v} pts</b></div><div className="mt-1.5 h-1.5 rounded-full bg-slate-100"><div className="h-full rounded-full bg-forest-600" style={{width:`${Number(v)*4}%`}}/></div></div>)}</div></section><section className="card rounded-2xl p-5"><BrainCircuit className="text-forest-700"/><h2 className="mt-3 font-black">AI service mode</h2><p className="mt-2 text-sm leading-6 text-slate-500">Deterministic prototype adapter. A production model can be connected through the same Edge Function interface.</p><Badge tone="medium" className="mt-4">Prototype · Not production CV</Badge></section><section className="card rounded-2xl p-5"><LockKeyhole className="text-forest-700"/><h2 className="mt-3 font-black">Security posture</h2><p className="mt-2 text-sm leading-6 text-slate-500">Supabase RLS policies enforce role boundaries. Protected media uses signed URLs in configured deployments.</p><div className="mt-4 flex items-center gap-2 text-xs font-bold text-forest-700"><Database size={15}/>PostgreSQL policies documented</div></section></div></div>}
